export type Subscription = {
  id: string
  name: string
  currency: string
  currency_value: number
  next_payment: string
  category: string
  icon_url: string
  created_at: string
  cycle: string
}
